//Ejercicio dentro de un rango

let dentroDeRango = 15;
let min = 0;
let max = 10;

if (dentroDeRango >= min && dentroDeRango <= max) {
  console.log(
    'El numero ' + dentroDeRango + ' Esta dentro del rango establecido'
  );
} else {
  console.log(
    'El numero ' + dentroDeRango + ' Esta fuera del rango establecido'
  );
}
